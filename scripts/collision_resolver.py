"""Behaviours for collisions between each pair of entities"""
from typing import TYPE_CHECKING
import logging

def collision_resolver(current_collision: tuple, remaining_pairs: list, proposed_map: "Map", collision_locs: list) -> None:
	logging.debug(msg="Ouch!")
	return
